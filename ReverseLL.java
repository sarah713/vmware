public class ReverseLL {
    Node reverseList(Node head) {
        // code here
        Node prev = null;
        while (head != null) {
            Node next = head.next;
            head.next = prev;
            prev = head;
            head = next;
        }
        return prev;
    }
}
